{% extends "layouts/layout.twig" %}

{% block hero %}

    <h1 class="hero__title">
        {{ text("hero__title", "Your mind is a powerful thing. When you fill it with positive thoughts your life will begin to change") }}
        {#        <span class="nobr">to change!</span>#}
    </h1>
    <p class="hero__text">
        So, choose your thoughts, focus and take action!
    </p>

{% endblock %}


{% block content %}

    <main class="main main--high container">

        <div class="post">
            <div class="post__content">
                <a href="/" class="post__home">
                    Home
                    <svg width="8" height="13" viewBox="0 0 8 13" fill="none" xmlns="http://www.w3.org/2000/svg">
                        <path d="M1 1L6.5 6.5L1 12" stroke="black"/>
                    </svg>
                </a>
                <div class="post__auto">

{#                    {% apply component("myComponent") %}#}

{#                    {% endapply %}#}

{#                    In tokenparser get a list of all names that need to be looked up #}

                    {{ text(local("SomeName")) }}
                    {{ text(global("Test")) }}

{#
    use {% global text("Some Name") %}{% endglobal %}
    or {% local text("Some Name") %}{% endlocal %}

    When using {{ text("Some Name }} it is automatically local


    {% k component("myComponent") %}
        // Component goes here
    {% endk %}

    {% component "myComponent" %}
        <h1>
            {{ text("Title") }}
        </h1>
        <p>
            {{ text("Content") }}
        </p>
    {% endComponent %}

    {% list("myList") %}
        {{ useComponent("myComponent", ["Title" => "My Title"]) }}
    {% endList %}

    {{
        list(["myComponent"], [
            ["Title" => "Test Title"],
            ["Title" => "Test title2"],
            ["Title" => "Test title3"]
        ])
     }}

    OR


    {% global %}

        {% All code here will be global (reset path before and reset after) %}

    {% endglobal %}

    {% group "myGroup" %}

    {% endGroup %}


    <div {{ list("myList") }}>
        <div {{ listItem("myList") }}>
            Another one bites the dust
        </div>
    </div>

#}


{#                    {% apply group("partners") %}#}
{#                    <div class="list" {{ list("partners") }}>#}
{#                        <div class="list__item" {{ listItem("partners") }}>#}
{#                            <h1 class="list__title">#}
{#                                {{ text("title", "Card title") }}#}
{#                                {{ textGroup("partners", "SomeName", "") }}#}
{#                            </h1>#}
{#                            <p class="list__desc">#}
{#                                {{ text("text", "Card text goes here") }}#}
{#                            </p>#}
{#                        </div>#}
{#                    </div>#}
{#                    {% endapply %}#}

{#                    {{ component("my-component") }}#}

{#                     {% apply group("some-group") %}#}
{#                         This is some random group#}
{#                         {{ text("Some text") }}#}
{#                     {% endapply %}#}

{#                    <form action="" {{ contact("my-form") }}>#}
{#                        <input name="someName" type="text" {{ contactInput("my-form") }}>#}
{#                        <button type="submit">Verzenden</button>#}
{#                    </form>#}

{#                    {{ wysiwyg("page_text") }}#}
                </div>
            </div>
        </div>

    </main>

{% endblock %}
